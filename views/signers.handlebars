<div class="titleContainer">
<h1 class="title">these people also want to have a turbo time</h1>
</div>

<div class="nameList">
{{#rows}}
<p> {{#if this.url}}
    <a href="{{this.url}}">{{this.first}} {{this.last}}</a>
    {{else}}
    {{this.first}} {{this.last}} 
    {{/if}}
    {{this.age}}
    {{#if this.city}} 
        <a href="/petition/signers/{{this.city}}">{{this.city}}</a>
    {{/if}}
    </p>
{{/rows}}

</div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>